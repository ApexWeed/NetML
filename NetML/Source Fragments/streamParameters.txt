void SetPars_stream_1()
{
    Ptr<Object> streamob;
    UintegerValue stream_1_SndBufSize_in_bytes1 = 100000;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_1");
    streamob = getInnerObject(net.Nnode_0, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_1 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("SndBufSize_in_bytes", stream_1_SndBufSize_in_bytes1))
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_1 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute SndBufSize_in_bytes of stream_1 has been set to 100000");
    }
    UintegerValue stream_1_MaxWindowSize_in_bytes2 = 65535;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_1");
    streamob = getInnerObject(net.Nnode_0, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_1 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("MaxWindowSize_in_bytes", stream_1_MaxWindowSize_in_bytes2))
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_1 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute MaxWindowSize_in_bytes of stream_1 has been set to 65535");
    }
    UintegerValue stream_1_SndBufSize_in_bytes3 = 100000;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_1");
    streamob = getInnerObject(net.Nnode_2, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_1 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("SndBufSize_in_bytes", stream_1_SndBufSize_in_bytes3))
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_1 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute SndBufSize_in_bytes of stream_1 has been set to 100000");
    }
    UintegerValue stream_1_MaxWindowSize_in_bytes4 = 65535;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_1");
    streamob = getInnerObject(net.Nnode_2, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_1 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("MaxWindowSize_in_bytes", stream_1_MaxWindowSize_in_bytes4))
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_1 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute MaxWindowSize_in_bytes of stream_1 has been set to 65535");
    }

}

void SetPars_stream_3()
{
    Ptr<Object> streamob;
    UintegerValue stream_3_SndBufSize_in_bytes1 = 100000;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_3");
    streamob = getInnerObject(net.Nnode_1, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_3 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("SndBufSize_in_bytes", stream_3_SndBufSize_in_bytes1))
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_3 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute SndBufSize_in_bytes of stream_3 has been set to 100000");
    }
    UintegerValue stream_3_MaxWindowSize_in_bytes2 = 65535;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_3");
    streamob = getInnerObject(net.Nnode_1, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_3 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("MaxWindowSize_in_bytes", stream_3_MaxWindowSize_in_bytes2))
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_3 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute MaxWindowSize_in_bytes of stream_3 has been set to 65535");
    }
    UintegerValue stream_3_SndBufSize_in_bytes3 = 100000;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_3");
    streamob = getInnerObject(net.Nnode_3, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_3 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("SndBufSize_in_bytes", stream_3_SndBufSize_in_bytes3))
    {
        NS_LOG_ERROR("No luck setting the attribute SndBufSize_in_bytes of stream_3 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute SndBufSize_in_bytes of stream_3 has been set to 100000");
    }
    UintegerValue stream_3_MaxWindowSize_in_bytes4 = 65535;

    // NS_LOG_INFO("About to look for an object of type TcpNewReno within the object net.Sstream_3");
    streamob = getInnerObject(net.Nnode_3, "TcpNewReno");
    if (!streamob)
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_3 because no TcpNewReno object was not found.");
    }
    else if (!streamob->SetAttributeFailSafe("MaxWindowSize_in_bytes", stream_3_MaxWindowSize_in_bytes4))
    {
        NS_LOG_ERROR("No luck setting the attribute MaxWindowSize_in_bytes of stream_3 because SetAttributeFailSafe failed.");
    }
    else
    {
        NS_LOG_INFO("The attribute MaxWindowSize_in_bytes of stream_3 has been set to 65535");
    }

}

void SetPars_stream_5()
{
    Ptr<Object> streamob;

}
